<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pink_A200</item>
        <item name="colorPrimaryDark">@color/pink_A700</item>
        <item name="colorAccent">@color/pink_A100</item>

        <item name="android:buttonStyle">@style/CustomButton</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomButton" parent="android:Widget.Button">
        <item name="android:background">@color/pink_200</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:height">48dp</item>
        <item name="android:padding">5dp</item>
    </style>
</resources>
